# Development dependencies — documentation only
# Install with: uv sync --extra dev

# Testing
pytest>=8.1.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0

# Linting, formatting, type checking (ruff handles lint + format, no black/isort needed)
ruff>=0.4.0
mypy>=1.9.0

# Pre-commit hooks
pre-commit>=3.7.0
